# APP_URL: URL where the app is hosted.
APP_URL="MY_APP_URL"

# Optional. If omitted, the app generates and stores a session secret in the DB.
# If set manually, use at least 8 characters.
ADMIN_SESSION_SECRET="troque-por-um-segredo-forte"
